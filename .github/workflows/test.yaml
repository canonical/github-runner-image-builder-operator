name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@enable-vale-style-checker-optionally
    secrets: inherit
    with:
      self-hosted-runner: false
      vale-style-checker: true
